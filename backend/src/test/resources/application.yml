spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        format_sql: true
    show-sql: true

logging:
  level:
    org.hibernate:
      SQL: debug
      type.descriptor.sql: trace