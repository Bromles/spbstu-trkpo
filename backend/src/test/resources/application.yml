spring:
  jpa:
    hibernate:
      ddl-auto: create
      properties:
        format_sql: true
    show-sql: true

logging:
  level:
    org.hibernate:
      SQL: debug
      type.descriptor.sql: trace