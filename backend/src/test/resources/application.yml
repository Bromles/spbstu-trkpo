spring:
  datasource:
    url: jdbc:tc:postgresql:16.0:///backend_db
    username: hospital
    password: password
  jpa:
    hibernate:
      ddl-auto: create
      properties:
        format_sql: true
    show-sql: true

logging:
  level:
    org.hibernate:
      SQL: debug
      type.descriptor.sql: trace